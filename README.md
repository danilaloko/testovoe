# Student Grades API

REST-сервис на FastAPI для загрузки и анализа успеваемости студентов.



## Архитектура


### Компоненты системы

#### 1. **API Layer** (`app/api/`)
- **`upload.py`** — обработка загрузки CSV-файлов
  - Валидация файла (размер, формат, кодировка)
  - Парсинг CSV с автоопределением разделителя
  - Валидация данных (ФИО, оценки)
  - Batch-вставка в БД
  
- **`students.py`** — аналитические эндпоинты
  - `/students/more-than-3-twos` — студенты с более чем 3 двойками
  - `/students/less-than-5-twos` — студенты с менее чем 5 двойками

#### 2. **Config Layer** (`app/config.py`)
- Централизованная конфигурация валидации
- Настройка через переменные окружения
- Валидация конфигурации при старте

#### 3. **DB Layer** (`app/db/`)
- **`connection.py`** — управление подключениями
  - Пул соединений (SimpleConnectionPool)
  - Retry-логика при подключении
  - Управление жизненным циклом соединений
  
- **`migrations.py`** — система миграций
  - Автоматическое применение миграций
  - Отслеживание примененных миграций
  - Версионирование схемы БД
  
- **`schema.py`** — схема БД (использует миграции)

#### 4. **Application Layer** (`app/main.py`)
- Инициализация FastAPI приложения
- Управление жизненным циклом (lifespan)
- Подключение роутеров
- Health check endpoint

---

## 🔧 Технические решения

### 1. Работа с базой данных

**Решение:** Использование чистого SQL через `psycopg2` без ORM

- **Пул соединений** (`SimpleConnectionPool`) для оптимизации производительности
- **Batch-вставка** данных (`executemany`) для эффективной загрузки больших объемов
- **Транзакции** с rollback при ошибках
- **Индексы** на `full_name` и `grade` для оптимизации запросов

**Пример SQL-запроса:**
```sql
SELECT full_name, COUNT(*) as count_twos
FROM grades
WHERE grade = 2
GROUP BY full_name
HAVING COUNT(*) > 3
ORDER BY count_twos DESC, full_name;
```

### 2. Система миграций

**Решение:** Кастомная система миграций на основе SQL-скриптов

- Миграции хранятся в папке `migrations/` с версионированием (`XXX_description.sql`)
- Отслеживание примененных миграций в таблице `schema_migrations`
- Автоматическое применение при старте приложения
- Идемпотентность через `IF NOT EXISTS` в SQL

**Структура миграций:**
```
migrations/
├── 000_init_schema_migrations.sql  # Инициализация системы миграций
└── 001_init.sql                     # Создание таблицы grades
```

### 3. Валидация данных

**Решение:** Многоуровневая валидация с настраиваемыми параметрами

**Уровни валидации:**
1. **Файл:** формат (CSV), размер, кодировка (UTF-8, Windows-1251)
2. **Структура CSV:** наличие обязательных полей, автоопределение разделителя
3. **Данные:** ФИО (длина, не пустое), оценки (диапазон, тип)

**Настройка через переменные окружения:**
- `MAX_FILE_SIZE_MB` — максимальный размер файла
- `MAX_ROWS` — максимальное количество строк
- `FULL_NAME_MIN_LENGTH` / `FULL_NAME_MAX_LENGTH` — ограничения ФИО
- `VALID_GRADES` — допустимые оценки
- `BATCH_SIZE` — размер батча для вставки

### 4. Обработка CSV

**Решение:** Автоопределение разделителя и поддержка разных кодировок

- Использование `csv.Sniffer` для автоопределения разделителя (`,`, `;`, `\t`)
- Поддержка кодировок UTF-8 и Windows-1251
- Обработка ошибок с детальными сообщениями
- Частичная загрузка при наличии ошибок (с предупреждениями)


### 5. Docker-интеграция

**Решение:** Multi-stage Dockerfile и Docker Compose

- **Multi-stage build** для оптимизации размера образа
- **Docker Compose** для оркестрации приложения и БД
- **Health checks** для мониторинга состояния сервисов
- **Volumes** для персистентности данных БД
- **Networks** для изоляции сервисов

### 6. Логирование

**Решение:** Структурированное логирование с использованием стандартного модуля `logging`

- Логирование всех операций (загрузка, запросы, ошибки)
- Разные уровни логирования (INFO, WARNING, ERROR)
- Форматирование с временными метками

### 7. Управление жизненным циклом

**Решение:** Использование `lifespan` вместо устаревших `@app.on_event()`

- Инициализация пула соединений при старте
- Применение миграций при старте
- Корректное закрытие соединений при остановке

---

## 📚 API Документация

### Базовый URL
```
http://localhost:8000
```

### Интерактивная документация
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

### Эндпоинты

#### POST `/upload-grades`

Загрузка CSV-файла с успеваемостью студентов.

**Валидация:**
- Файл должен быть в формате CSV
- Максимальный размер файла: 10 МБ
- Максимальное количество строк: 100,000
- Обязательные поля: `full_name`, `grade`
- ФИО: не пустое, минимум 2 символа, максимум 255 символов
- Оценка: целое число из списка допустимых (по умолчанию: 2, 3, 4, 5)

Все параметры валидации настраиваются в .env


**Пример запроса:**
```bash
curl -X POST "http://localhost:8000/upload-grades" \
  -H "accept: application/json" \
  -H "Content-Type: multipart/form-data" \
  -F "file=@grades.csv"
```

**Ответ при успехе:**
```json
{
  "status": "ok",
  "records_loaded": 2000,
  "students": 40
}
```

**Ответ с предупреждениями:**
```json
{
  "status": "ok",
  "records_loaded": 1950,
  "students": 40,
  "warnings": "Обнаружено 50 ошибок при обработке",
  "error_details": [
    "Строка 10: оценка должна быть 2, 3, 4 или 5",
    "Строка 15: ФИО не может быть пустым"
  ]
}
```

#### GET `/students/more-than-3-twos`

Возвращает студентов, у которых оценка 2 встречается больше 3 раз.

**Пример запроса:**
```bash
curl -X GET "http://localhost:8000/students/more-than-3-twos"
```

**Ответ:**
```json
[
  {
    "full_name": "Иванов Иван Иванович",
    "count_twos": 5
  },
  {
    "full_name": "Петров Пётр Петрович",
    "count_twos": 4
  }
]
```

#### GET `/students/less-than-5-twos`

Возвращает студентов, у которых оценка 2 встречается меньше 5 раз.

**Пример запроса:**
```bash
curl -X GET "http://localhost:8000/students/less-than-5-twos"
```

**Ответ:**
```json
[
  {
    "full_name": "Сидоров Сидор Сидорович",
    "count_twos": 2
  },
  {
    "full_name": "Козлов Козел Козлович",
    "count_twos": 0
  }
]
```

#### GET `/health`

Health check endpoint для мониторинга состояния сервиса.

**Ответ:**
```json
{
  "status": "healthy"
}
```

---

## 🚀 Установка и запуск

### Требования

- Python 3.8+
- PostgreSQL 12+
- Docker и Docker Compose (опционально, для Docker-варианта)

### Вариант 1: Запуск через Docker Compose

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd testovoe

# Запуск приложения и базы данных
docker-compose up -d

# Просмотр логов
docker-compose logs -f app

# Остановка
docker-compose down

# Остановка с удалением данных БД
docker-compose down -v
```

Сервис будет доступен по адресу: http://localhost:8000

### Вариант 2: Локальный запуск (для разработки)

#### 1. Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate  # для Linux/Mac
# или
venv\Scripts\activate  # для Windows
```

#### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

#### 3. Настройка PostgreSQL

**Вариант А: Через Docker**

```bash
# Запуск PostgreSQL через docker-compose
docker-compose up -d postgres

# Или через docker run
docker run --name postgres-student-grades \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=student_grades \
  -p 5432:5432 \
  -d postgres:16
```

**Вариант Б: Подключение к удаленному серверу**

Настройте переменные окружения в `.env` файле с параметрами удаленного сервера.

#### 4. Создание базы данных

```bash
# Для локального сервера
createdb student_grades

# Или через psql
psql -U postgres -c "CREATE DATABASE student_grades;"
```

#### 5. Настройка переменных окружения

```bash
# Создайте файл .env на основе примера
cp .env.example .env

# Отредактируйте .env файл с вашими настройками БД
```

**Пример `.env` файла:**
```env
# Параметры подключения к БД
DB_HOST=localhost
DB_PORT=5432
DB_NAME=student_grades
DB_USER=postgres
DB_PASSWORD=postgres

# Параметры валидации CSV (опционально)
MAX_FILE_SIZE_MB=10
MAX_ROWS=100000
BATCH_SIZE=1000
FULL_NAME_MIN_LENGTH=2
FULL_NAME_MAX_LENGTH=255
VALID_GRADES=2,3,4,5
CSV_FIELD_FULL_NAME=full_name
CSV_FIELD_GRADE=grade
```

#### 6. Применение миграций

```bash
# Вариант 1: Через скрипт миграций (рекомендуется)
python migrate.py

# Вариант 2: Через скрипт инициализации
python init_db.py
```

**Примечание:** Миграции применяются автоматически при первом запуске приложения.

#### 7. Запуск приложения

```bash
# Убедитесь, что PostgreSQL запущен
docker-compose up -d postgres  # если используете Docker

# Активируйте виртуальное окружение
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

# Запустите приложение
uvicorn app.main:app --reload
```

Сервис будет доступен по адресу: http://localhost:8000

---

## 📁 Структура проекта

```
testovoe/
├── app/                          # Основное приложение
│   ├── __init__.py
│   ├── main.py                   # Точка входа FastAPI приложения
│   ├── config.py                 # Конфигурация валидации
│   ├── api/                      # API эндпоинты
│   │   ├── __init__.py           # Роутер API
│   │   ├── upload.py             # POST /upload-grades
│   │   └── students.py           # GET /students/*
│   └── db/                       # Работа с базой данных
│       ├── __init__.py
│       ├── connection.py         # Пул соединений с БД
│       ├── migrations.py         # Система миграций
│       └── schema.py             # Схема БД (использует миграции)
│
├── migrations/                   # SQL-скрипты миграций
│   ├── 000_init_schema_migrations.sql  # Инициализация системы миграций
│   ├── 001_init.sql              # Создание таблицы grades
│   └── README.md                 # Документация по миграциям
│
├── scripts/                      # Вспомогательные скрипты
│   ├── upload_csv.py             # Скрипт для тестирования загрузки CSV
│   └── students_grades.csv       # Пример CSV файла
│
├── docker-compose.yml            # Docker Compose конфигурация
├── Dockerfile                    # Docker образ приложения
├── init_db.py                    # Скрипт инициализации БД
├── migrate.py                    # Скрипт применения миграций
├── requirements.txt              # Python зависимости
├── README.md                     # Документация проекта
└── DOCKER.md                     # Детальные инструкции по Docker
```

---

## 🗄️ База данных

### Схема базы данных

#### Таблица `grades`

Хранит оценки студентов.

```sql
CREATE TABLE grades (
    id SERIAL PRIMARY KEY,
    full_name VARCHAR(255) NOT NULL,
    grade INTEGER NOT NULL CHECK (grade IN (2, 3, 4, 5)),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**Индексы:**
- `idx_grades_full_name` на поле `full_name`
- `idx_grades_grade` на поле `grade`

#### Таблица `schema_migrations`

Отслеживает примененные миграции.

```sql
CREATE TABLE schema_migrations (
    version VARCHAR(255) PRIMARY KEY,
    description TEXT,
    applied_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Миграции

Проект использует систему миграций на основе SQL-скриптов:

- Все миграции находятся в папке `migrations/`
- Миграции применяются автоматически при старте приложения
- Можно применить миграции вручную: `python migrate.py`
- Примененные миграции отслеживаются в таблице `schema_migrations`

**Создание новой миграции:**

1. Создайте файл в папке `migrations/` с именем `XXX_description.sql` (где XXX - порядковый номер)
2. Напишите SQL-код для изменения схемы БД
3. Запустите `python migrate.py` для применения

**Пример:**
```sql
-- migrations/002_add_email_column.sql
ALTER TABLE grades ADD COLUMN email VARCHAR(255);
```

Подробнее см. [migrations/README.md](migrations/README.md)

---

## ⚙️ Конфигурация

Параметры валидации CSV файлов настраиваются через переменные окружения в файле `.env`.

Все параметры имеют значения по умолчанию, поэтому настройка опциональна.

### Параметры подключения к БД

- `DB_HOST` — хост БД (по умолчанию: `localhost`)
- `DB_PORT` — порт БД (по умолчанию: `5432`)
- `DB_NAME` — имя БД (по умолчанию: `student_grades`)
- `DB_USER` — пользователь БД (по умолчанию: `postgres`)
- `DB_PASSWORD` — пароль БД (по умолчанию: `postgres`)

### Параметры валидации CSV

- `MAX_FILE_SIZE_MB` — максимальный размер файла в МБ (по умолчанию: `10`)
- `MAX_ROWS` — максимальное количество строк (по умолчанию: `100000`)
- `FULL_NAME_MIN_LENGTH` — минимальная длина ФИО (по умолчанию: `2`)
- `FULL_NAME_MAX_LENGTH` — максимальная длина ФИО (по умолчанию: `255`)
- `VALID_GRADES` — допустимые оценки через запятую (по умолчанию: `2,3,4,5`)
- `BATCH_SIZE` — размер батча для вставки в БД (по умолчанию: `1000`)
- `CSV_FIELD_FULL_NAME` — название поля ФИО в CSV (по умолчанию: `full_name`)
- `CSV_FIELD_GRADE` — название поля оценки в CSV (по умолчанию: `grade`)

---

## 🧪 Тестирование

### Тестирование загрузки CSV

Для тестирования загрузки CSV файла можно использовать скрипт:

```bash
# Использование примера CSV файла
python scripts/upload_csv.py scripts/students_grades.csv

# Использование своего файла
python scripts/upload_csv.py path/to/your/file.csv
```

---